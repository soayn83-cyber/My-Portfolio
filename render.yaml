services:
  - type: web
    name: soayhyeon05-web
    env: node
    plan: free
    branch: main
    rootDir: .
    buildCommand: npm run build
    startCommand: npm start
    healthCheckPath: /api/status
    autoDeploy: true
